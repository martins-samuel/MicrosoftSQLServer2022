# 📘 **5º Capítulo — Auto Incremento, Padrões e Triggers**

**Resumo do capítulo:**

- Aplicar campos com **auto-incremento** para gerar valores automáticos em registros.
- Definir **valores padrão (DEFAULT)** para colunas, garantindo consistência no preenchimento.
- Utilizar **TRIGGERS** para executar ações automáticas no banco de dados em eventos como `INSERT`, `UPDATE` e `DELETE`.
- Usar propriedades de **validação** na tabela para reforçar as regras de integridade.
- Identificar e aplicar manipulação de dados com ferramentas de **ETL** e rotinas de **programação**.


